# ponytail: hoist @types/* para node_modules/ da raiz do monorepo.
# Sem isto, pacotes dentro de .pnpm/ resolvem @types/react subindo para fora do repo
# (workspace/2026/node_modules) e o tsc vê duas cópias de React ("unique symbol" em key).
public-hoist-pattern[]=@types/*
